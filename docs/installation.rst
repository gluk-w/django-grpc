============
Installation
============

At the command line::

    $ easy_install django-grpc

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-grpc
    $ pip install django-grpc
